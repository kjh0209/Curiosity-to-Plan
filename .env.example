# Database
DATABASE_URL="file:./dev.db"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="여기에32자이상의랜덤시크릿을입력하세요"

# ===========================================
# Gemini AI Configuration (Key Pool)
# ===========================================

# 여러 Google Cloud 프로젝트의 API 키를 쉼표로 구분하여 입력
# 각 키는 독립된 프로젝트의 free tier 쿼터를 사용하므로
# 키가 많을수록 더 많은 무료 요청 처리 가능
# https://aistudio.google.com/app/apikey 에서 각각 다른 Google 계정으로 발급
GEMINI_API_KEYS="key1_from_project_A,key2_from_project_B,key3_from_project_C"

# 단일 키 폴백 (GEMINI_API_KEYS가 없을 때 사용)
GEMINI_API_KEY="여기에_Gemini_API_키_입력"

# ===========================================
# YouTube Data API v3 (Key Pool)
# ===========================================

# 여러 Google Cloud 프로젝트의 YouTube API 키를 쉼표로 구분
# 각 키는 하루 10,000 units (검색 1회 = 100 units = 키당 ~100회/일)
# https://console.cloud.google.com/apis/credentials 에서 발급
YOUTUBE_API_KEYS="yt_key1,yt_key2,yt_key3"

# 단일 키 폴백
YOUTUBE_API_KEY="여기에_YouTube_API_키_입력"

# ===========================================
# OpenAI (Optional)
# ===========================================
# 사용자가 프로필에서 직접 입력하므로 서버 키는 선택사항
# OPENAI_API_KEY="sk-..."
